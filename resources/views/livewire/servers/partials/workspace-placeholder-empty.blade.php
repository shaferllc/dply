{{-- Minimal single-root lazy placeholder used when no server context is available yet. --}}
<div class="min-h-[40vh]" aria-busy="true" aria-live="polite">
    <span class="sr-only">{{ __('Loading…') }}</span>
</div>

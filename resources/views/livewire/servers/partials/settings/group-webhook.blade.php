{{--
    DEPRECATED — the per-server outbound webhook moved to the Notifications page
    (Notifications → Integration webhooks), owned by the ManagesServerWebhook
    trait and rendered via partials/notifications/_outbound-webhook.blade.php.
    The old /servers/{server}/settings/webhook URL now redirects there.

    This file is no longer @include'd anywhere and can be safely `git rm`'d.
--}}

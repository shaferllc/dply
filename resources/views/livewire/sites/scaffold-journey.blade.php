{{-- Legacy standalone scaffold page — just the centered page chrome around
     the shared scaffold-install partial. The product flow renders the same
     partial inside the site workspace shell (Show). --}}
<div class="mx-auto max-w-3xl px-4 py-10 sm:px-6 lg:px-8">
    @include('livewire.sites.partials.show.scaffold-install-journey')
</div>

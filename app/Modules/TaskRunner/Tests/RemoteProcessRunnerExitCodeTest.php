<?php

declare(strict_types=1);

// Moved to tests/Feature/TaskRunner/RemoteProcessRunnerExitCodeTest.php — the
// module Tests/ directory is not part of the phpunit testsuites, so the
// regression test lives where `composer test` actually executes it.
